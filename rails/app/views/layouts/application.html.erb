<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= favicon_link_tag 'favicon.ico' %>
    <title><%= content_for?(:title) ? yield(:title) : 'Quiche' %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <%= yield %>

    <div class='login_button'>
      <%= flash.notice %>
      <% if current_user %>
        Welcome @<%= current_user.twitter_id %>!
        <%= link_to 'Sign Out', signout_path %>
      <% else %>
        <%= link_to 'Login (members only)', '/auth/twitter' %>
      <% end %>
    </div>
  </body>
</html>